<div id="bizContainer">
  <h3 style="color:#aaa;text-align:center;">FEATURED ON:</h3>

  <div class="articleImageContainer">
  <% @logo.each do |logo| %>
    <img class="articleImage" src="<%= 'https://s3-us-west-1.amazonaws.com/wespot-photo-uploads/uploads/logos/' + "#{logo.name}" + ".png" %>" />
    <% end %>
  </div>
</div>
