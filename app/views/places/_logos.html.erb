<div id="logoContainer">

  <% @logo.each do |logo| %>
  <div class="logoImage">
    <img style="height:30px;" src="<%= 'https://s3-us-west-1.amazonaws.com/wespot-photo-uploads/uploads/logos/' + "#{logo.name}" + ".png" %>" />
  </div>
    <% end %>
</div>
